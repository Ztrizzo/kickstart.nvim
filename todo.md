# List of things to improve:
  Command for new terminal (<leader>T) should only create new terminal tab if it doens't already exist. If it exists, jump to that tab. Maybe can use global tags?

# Salesforce
  Push shouldn't work if not in a SF directory. Maybe executes file instead?
